# ai-engine – Service AI

Chưa chọn công nghệ. Khi bắt đầu cần có:

- `Dockerfile` – lắng nghe cổng `8000`
- API nội bộ để backend gọi (app Flutter không gọi trực tiếp)
- Đọc API key từ biến môi trường `LLM_API_KEY` (nếu dùng LLM API)
- Bỏ comment service `ai-engine` trong `docker-compose.yml`
